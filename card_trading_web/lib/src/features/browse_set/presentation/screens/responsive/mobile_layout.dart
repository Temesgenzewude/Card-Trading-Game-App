import 'package:flutter/material.dart';

class BrowseSetsMobile extends StatelessWidget {
  const BrowseSetsMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(),
    );
  }
}
