import 'package:flutter/material.dart';

class BrowseSetsTablet extends StatelessWidget {
  const BrowseSetsTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(),
    );
  }
}